/* 底部滑出面板样式 - Apple 风格 */

.bottom-sheet {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  z-index: 1000;
  visibility: hidden;
  pointer-events: none;
}

.bottom-sheet.show {
  visibility: visible;
  pointer-events: auto;
}

/* 遮罩层 */
.mask {
  position: absolute;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: rgba(0, 0, 0, 0);
  transition: background 0.3s ease;
}

.mask.mask-show {
  background: rgba(0, 0, 0, 0.5);
}

/* 面板内容 */
.sheet-content {
  position: absolute;
  left: 0;
  right: 0;
  bottom: 0;
  background: #ffffff;
  border-radius: 40rpx 40rpx 0 0;
  transform: translateY(100%);
  transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
  max-height: 90vh;
  overflow: visible;
  display: flex;
  flex-direction: column;
}

.sheet-content.sheet-show {
  transform: translateY(0);
}

/* 标题栏 */
.sheet-header {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 48rpx 40rpx 24rpx;
  flex-shrink: 0;
}

.sheet-title {
  font-size: 34rpx;
  font-weight: 600;
  color: #1d1d1f;
}

.sheet-cancel {
  font-size: 34rpx;
  color: #007aff;
}

/* 分割线 */
.sheet-divider {
  height: 1rpx;
  background: rgba(0, 0, 0, 0.1);
  margin: 0 40rpx;
  flex-shrink: 0;
}

/* 内容区域 */
.sheet-body {
  padding: 32rpx 40rpx;
  flex: 1;
  overflow-y: auto;
  overflow-x: visible;
  box-sizing: border-box;
}

/* 暗色模式 */
.bottom-sheet.dark .sheet-content {
  background: #1c1c1e;
}

.bottom-sheet.dark .sheet-title {
  color: #f5f5f7;
}

.bottom-sheet.dark .sheet-cancel {
  color: #0a84ff;
}

.bottom-sheet.dark .sheet-divider {
  background: rgba(255, 255, 255, 0.1);
}
